// Tower shield: tall rectangular wall. Heaviest to wield — full damage
// nullification and the widest arc, but the slowest recovery and the
// stiffest stamina/movement penalties.
export default Object.freeze({
  size: "large",
});
