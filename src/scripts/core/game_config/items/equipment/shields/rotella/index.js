// Italian Renaissance rotella: ornate ceremonial round, slightly larger than
// a buckler. Engraved steel face — covers a touch wider arc than its smaller
// peers, at the cost of marginally heavier stamina draw.
export default Object.freeze({
  size: "small",
  metallic: true,
  blockAngle: Math.PI * 0.84,
  staminaCost: 1.2,
  damageReduction: 0.78,
});
