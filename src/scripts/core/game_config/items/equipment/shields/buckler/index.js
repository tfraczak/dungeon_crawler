// Smallest shield. Pulls block stats from SIZE_DEFAULTS.small in the parent
// shields config; nothing about the buckler differs from the small baseline
// other than its sprite/look.
export default Object.freeze({
  size: "small",
  metallic: true,
});
